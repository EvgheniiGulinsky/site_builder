// Payment feature
export const processPayment = (amount: number, currency: string, provider: string) => {
  console.log(`Processing payment of ${amount} ${currency} via ${provider}`);
  // Logic for payment processing
};